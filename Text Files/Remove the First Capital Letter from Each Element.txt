#!/bin/bash

# Read input into an array
readarray -t countries

# Loop through array and replace first capital letter with '.'
for i in "${!countries[@]}"; do
    countries[$i]="${countries[$i]/[A-Z]/.}"
done

# Print the transformed array with spaces
echo "${countries[@]}"